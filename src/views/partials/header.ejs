<header>
  <nav class="navbar">
       <a class="nav-link title" href="/">Online bookstore - Home</a>
       <a class="nav-link" href="book-search">Our Books</a>
       <% if (typeof userId !== 'undefined' && userId) { %>
        <a class="nav-link auth" href="auth/logout">Log Out</a>
      <% } else { %>
        <a class="nav-link auth" href="auth/register">Register</a>
        <a class="nav-link auth" href="auth/login">Log In</a>
      <% } %>
  </nav>
</header>